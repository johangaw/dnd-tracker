name: Deploy

on:
  push:
    branches: [main]
  # Lets you re-deploy the current main without pushing a commit.
  workflow_dispatch:

# Two deploys at once would race on the CDK stack, the S3 sync and the
# invalidation.
concurrency:
  group: deploy
  cancel-in-progress: false

permissions:
  # Required to request the OIDC token that AWS exchanges for role credentials.
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: ${{ steps.deploy.outputs.site_url }}
    env:
      AWS_REGION: ${{ vars.AWS_REGION || 'eu-north-1' }}
      APP_NAME: ${{ vars.APP_NAME || 'dnd-tracker' }}
      # The stack whose outputs deploy.sh reads to upload content.
      STACK_NAME: ${{ vars.APP_NAME || 'dnd-tracker' }}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: |
            package-lock.json
            infra/package-lock.json

      - run: npm ci

      # A broken build must never reach the site.
      - name: Run tests
        run: npm test

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      # CI owns the infrastructure as well as the content, so pushing to main is
      # the single path to production for both.
      - name: Install CDK dependencies
        run: npm ci
        working-directory: infra

      # The application stack only. The OIDC stack is deliberately excluded:
      # it defines the very role this job is running as.
      - name: Deploy infrastructure
        run: npx cdk deploy "$APP_NAME" --require-approval never
        working-directory: infra

      - name: Deploy content
        id: deploy
        run: |
          ./infra/deploy.sh | tee deploy.log
          echo "site_url=$(grep -oE 'https://[a-z0-9]+\.cloudfront\.net' deploy.log | tail -1)" >> "$GITHUB_OUTPUT"
